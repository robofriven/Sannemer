// I'm thinking that this will have the stuff regarding the card back.  Not really sure what will need to be done quite yet, but this is here in case I need it.

using UnityEngine;
using System.Collections;

public class CardFrame : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
